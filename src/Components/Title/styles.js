import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 16,
    color: 'black',
  //marginTop: 4,
    fontWeight:'bold',

  },
});

export default styles;
